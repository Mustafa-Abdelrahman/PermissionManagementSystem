@model List<string>
@using Microsoft.AspNetCore.Identity
@using PermissionManagement.Web.Constants
@using System.Security.Claims
@using Microsoft.AspNetCore.Authorization
@inject IAuthorizationService AuthorizationService


@if ((await AuthorizationService.AuthorizeAsync(User, "Access Block1")).Succeeded)
{
    <div class="card" style="width: 18rem;">
        <div class="card-body">
            <h3 class="card-title">@Blocks.Block1.ToString()</h3>
        </div>
    </div>
}

@if ((await AuthorizationService.AuthorizeAsync(User, "Access Block2")).Succeeded)
{
    <div class="card" style="width: 18rem;">
        <div class="card-body">
            <h3 class="card-title">@Blocks.Block2.ToString()</h3>
        </div>
    </div>
}

@if ((await AuthorizationService.AuthorizeAsync(User, "Access Block3")).Succeeded)
{
    <div class="card" style="width: 18rem;">
        <div class="card-body">
            <h3 class="card-title">@Blocks.Block3.ToString()</h3>
        </div>
    </div>
}

@Html.ActionLink("Back to Members Area", "Index")

